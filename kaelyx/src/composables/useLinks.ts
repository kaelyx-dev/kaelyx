import { useConfig } from "@composable/useConfig"

export const useLinks = () => {

    const config = useConfig()

    const getLink = (linkName: string) => {

    }

    const getGroup = (linkGroup: string) => {
        
    }

    return { getLink, getGroup }
}